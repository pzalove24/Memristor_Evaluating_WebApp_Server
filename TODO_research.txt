invent => instruction set architecture for non-vonneuman or neuromorphic architecture

Number of download
Profile of contributor

Capacitor nextjs

set template for benchmark => draggable อย่าลืมเตือนด้วยว่า Endurance, Reliability จะ damage device

output ที่ได้จาก method สามารถใช้ซ้ำในการหา calculation หรือ graph อื่นๆได้
-method หลัก =>> method รอง ที่สามารถใช้ output ที่ได้จาก board ไปคำนวณหาค่าตัวอื่นได้ หรือ หา graph อื่นๆได้

filter method อาจเปลี่ยนจาก graph/calculation เป็น benchmark Input ที่ใช้แทน เพราะมันใช้ซ้ำกันได้

ใช้ event ในการทำ event log result จาก hardware
ดู example result
mock data

เก็บ json
เก็บ element ใน json บน database
method
submethod

ในหน้า benchmark operation มี tool tip ดูว่าจะได้ result อะไรบ้าง
submethod เป็น card ดีไหม แล้วสามารถคลิ๊กที่ card เพื่อดู example result ได้ ว่าเอาข้อมูลจาก method มาใช้ยังไง

submethod main, sub
main => output to graph or calculation => submain from output


frontend จะใช้ data เฉพาะใน graph หรือเป็นตัวเลขไป
backend จะจัดการ serialport ที่เข้ามา แล้ว convert ไปเป็น data ที่สามารถแสดงใน frontend โดยใช้ json file location ที่เก็บใน database แล้วส่งเป็น array data กลับไป frontend

button to click to animate example result shown in sequence (ทั้งใน method และ template)

ใช้ backdrop กับ progress circle แบบมีตัวเลข ในการรอ result กับ example result
ใช้ transition เปลี่ยนจาก cardimage เป็น graph หรือ calculation ได้ => transition ทำ delay ได้
https://medium.com/@vishal.s.katharotiya/creating-a-real-time-progress-bar-with-react-and-node-js-fb7c3c55ba8

example result เริ่มตั้งแต่ แปลงข้อมูลจาก csv เป็น data ที่ graph หรือ calculation อ่านด้าน

อาจทำ class event graph transform กับ calculation transform

#15318e color
//